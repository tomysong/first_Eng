# AI초딩영어 v2 업데이트 기획

## 먼저 확인한 음성 인식 상태

- 브라우저 앱은 `http://127.0.0.1:5173/`에서 정상 로딩된다.
- 오늘 화면의 `말하기`, `내 목소리 녹음`, `내 발음 ↔ 원어민 비교` 버튼은 정상 렌더링된다.
- STT 흐름은 `MediaRecorder` 녹음 → `modules/stt.js` → Worker `/openai/v1/audio/transcriptions` → `gpt-4o-mini-transcribe` 구조다.
- Worker에는 OpenAI 전사 경로, 6MB 오디오 제한, rate limit hook이 구현되어 있다.
- 로컬 테스트 중 발견한 수정점: Worker 허용 Origin에 `5173`이 없어 로컬 전사 POST가 차단될 수 있었다. `proxy/cloudflare-worker.js`에 `localhost/127.0.0.1`의 `5173`, `8000`을 추가했다.
- 실제 아이 목소리 인식 품질은 사용 기기에서 마이크 권한을 허용하고, 오늘 화면 `말하기`로 2~3문장을 녹음해 전사 결과를 봐야 최종 판단할 수 있다.

## v2 한 줄 방향

`My World`: 실제 친구 또는 같은 기기의 다른 프로필과 영어로 주고받고, 게임 미션으로 반복하며, 내 발음이 원어민 발음과 어떻게 다른지 바로 듣고 고치는 버전.

## 핵심 업데이트 3가지

### 1. 실제 친구/다른 프로필과의 대화

- 기존 AI 캐릭터 대화와 별개로 `친구랑 하기` 모드를 추가한다.
- 한 기기를 같이 쓰는 경우, 이미 있는 프로필 중 2명을 골라 `A 역할`, `B 역할`로 대화한다.
- 각자 기기가 있는 경우는 서버 동기화 전까지 `한쪽이 문제를 만들고 상대가 답하기` 식의 패스앤플레이/코드 공유형으로 시작한다.
- AI는 대화 상대가 아니라 `심판/코치` 역할만 맡는다. 예: 문장 추천, 발음 확인, 점수/배지 판정.
- 추천 상황:
  - 새 친구에게 자기소개하기
  - 쉬는 시간에 좋아하는 것 묻기
  - 친구를 게임/놀이에 초대하기
  - 학교생활 이야기하기
  - 취미와 주말 계획 말하기
- 각 상황은 `A가 말할 표현`, `B가 답할 표현`, `서로 물어볼 질문`, `성공 조건`을 가진다.

#### 친구 대화 모드 후보

- `같은 기기 패스앤플레이`: 프로필 A가 말하고, 기기를 넘기면 프로필 B가 답한다.
- `역할 카드 대화`: 화면에 A/B 역할 카드가 나오고, 각자 자기 문장을 말하면 체크된다.
- `질문 주고받기`: A가 질문을 말해야 B의 답변 카드가 열린다.
- `친구 인터뷰`: 서로 3개 질문을 영어로 묻고 답하면 인터뷰 카드가 완성된다.
- `비밀 미션`: 각자 몰래 받은 목표 표현을 대화 중 자연스럽게 말하면 성공한다.

### 2. 게임 요소 추가

- 표현 카드에서 한 단계 더 나아가 `미션 게임` 탭 또는 오늘 화면의 게임 블록을 만든다.
- 첫 v2 게임 후보:
  - `친구 말하기 빙고`: 두 프로필이 번갈아 목표 표현을 말하면 칸이 열린다.
  - `대화 핑퐁`: 질문→답변을 끊기지 않고 이어가면 콤보가 오른다.
  - `문장 배틀`: 같은 한국어 미션을 보고 누가 더 정확히 영어로 말하는지 겨룬다.
  - `협동 보스`: 두 명이 각자 2문장씩 말해야 보스 미션이 클리어된다.
  - `단어 보물찾기`: 발화 전사 결과에 목표 단어가 들어 있으면 보상 획득.
  - `스피드 초대`: 제한 시간 안에 친구를 영어로 초대하고 답변까지 받기.
- 보상은 점수보다 `둘이 딴 배지`, `우정 카드`, `화분 장식`처럼 같이 모으는 수집형으로 둔다.

### 3. 원어민-내 발음 비교 고도화

- 현재 기능은 `내 녹음 재생 → 원어민 TTS 재생`까지 가능하다.
- v2에서는 비교 결과를 화면에 남긴다.
- 추가할 피드백:
  - 내가 말한 문장 전사 결과
  - 목표 문장과 단어별 일치 표시
  - 놓친 단어 1~2개만 다시 듣기
  - `천천히 듣기`, `원어민 먼저`, `내 목소리 먼저` 순서 전환
  - 최근 녹음 3개 저장 후 다시 듣기
- 발음 점수는 과하게 숫자화하지 않고 `잘 들렸어요`, `한 번 더`, `이 단어만 살려봐요`처럼 코칭형으로 둔다.

## v2 28일 커리큘럼 초안

| 주차 | 주제 | 말하기 목표 | 대표 미션 |
| --- | --- | --- | --- |
| Week 1 | About Me | 이름, 나이, 좋아하는 것, 성격 말하기 | 두 프로필 자기소개 핑퐁 |
| Week 2 | Friends | 친구 소개, 같이 하는 활동, 초대하기 | 친구에게 놀자고 말하기 |
| Week 3 | School Life | 과목, 쉬는 시간, 급식, 숙제 이야기 | 학교 하루 인터뷰 |
| Week 4 | Hobbies | 게임, 운동, 음악, 그림, 주말 계획 | 취미 소개 협동 보스 |

## 개발 우선순위

1. 로컬 음성 인식 실기 테스트: 아이 목소리 3문장 녹음, 전사 정확도와 오류 패턴 기록.
2. 발음 비교 UI 저장: 전사 결과와 단어별 피드백을 `내 발음 ↔ 원어민 비교` 아래에 남기기.
3. 두 프로필 선택 UI: `친구랑 하기` 시작 시 프로필 A/B를 고르게 한다.
4. 패스앤플레이 대화 MVP: A가 말하면 B 차례로 넘어가고, 각 발화를 STT로 체크한다.
5. 친구 말하기 빙고 MVP: 목표 표현 9칸, 두 프로필 발화 키워드 매칭으로 칸 열기.
6. v2 데이터 추가: `v2Curriculum`, `v2Dialogues`, `v2FriendMissions`를 `modules/data.js`에 분리 추가.
7. 보호자 리포트: 이번 주 말한 문장, 잘한 표현, 다시 할 표현을 프로필별/함께 보기로 요약.

## 품질 기준

- 초등 5학년이 한 손으로 바로 누를 수 있어야 한다.
- 실패 문구는 짧고 다정해야 한다.
- 게임은 영어 말하기를 대신하지 않고, 말하기를 더 자주 하게 만드는 장치여야 한다.
- 친구 모드는 경쟁보다 협동을 기본값으로 둔다. 이겨서 끝나는 게임보다 같이 성공하는 게임이 오래 간다.
- 발음 비교는 아이를 평가하는 느낌보다 “다시 해보고 싶다”는 느낌을 줘야 한다.
